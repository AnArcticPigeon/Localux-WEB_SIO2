{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
<div class="card" style="width: 75%; margin-top:10%">
     <div class="card-body">
     <center><h2>Inscription</h2></center>
        {{ form_start(formAddUtilisateur) }}
        {{ form_row(formAddUtilisateur.nom) }}
        {{ form_row(formAddUtilisateur.prenom) }}
        {{ form_row(formAddUtilisateur.email) }}
        {{ form_row(formAddUtilisateur.mdp) }}
        <center>
            <button type="submit" class="btn btn-primary" name="btnInscription">Créer</button>
        </center>
        {{ form_end(formAddUtilisateur) }}
    </div>
</div>

</div>
{% endblock %}
